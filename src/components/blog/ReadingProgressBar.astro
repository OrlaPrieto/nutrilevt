---
---
<div
  id="reading-progress-container"
  class="fixed top-0 left-0 w-full h-1 z-[100] bg-black/10 pointer-events-none"
>
  <div
    id="reading-progress-bar"
    class="h-full w-0 transition-[width] duration-150 ease-out rounded-r-full"
    style="background: linear-gradient(90deg, #10B981 0%, #14B8A6 50%, #06B6D4 100%); box-shadow: 0 1px 8px rgba(16, 185, 129, 0.7), 0 0 12px rgba(6, 182, 212, 0.6);"
  >
  </div>
</div>

<script>
  function initReadingProgressBar() {
    const progressBar = document.getElementById('reading-progress-bar');
    if (!progressBar) return;

    const updateProgress = () => {
      const scrollY = window.scrollY || document.documentElement.scrollTop;
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      if (docHeight <= 0) {
        progressBar.style.width = '0%';
        return;
      }
      const progressPercent = Math.min(100, Math.max(0, (scrollY / docHeight) * 100));
      progressBar.style.width = `${progressPercent}%`;
    };

    window.addEventListener('scroll', updateProgress, { passive: true });
    updateProgress();
  }

  document.addEventListener('astro:page-load', initReadingProgressBar);
  initReadingProgressBar();
</script>
